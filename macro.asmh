%ifndef MACRO_ASMH
%define MACRO_ASMH

%macro pushqw 1
    sub sp, 8
    push eax
    add sp, 12

    mov rax, %1

    push eax
    shr rax, 16
    push eax

    sub sp,  4
	pop eax
%endmacro

%macro popqw 1
	pop %1
    shl %1, 16
    pop %1
%endmacro

%endif

